[project]
name = "matml"
version = "0.1.0"
description = "Dockerfiles for MatML ecosystem."
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[dependency-groups]
dev = [
    "invoke>=2.2.0",
    "pre-commit>=4.2.0",
]
